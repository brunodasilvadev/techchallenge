apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: api
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: portahttp
  - port: 443
    targetPort: 443
    name: portahttps
  selector:
    app: api